import React, { useState } from 'react'

const Options = () => {
    const [isActive,setIsActive] = useState(false);
    
  return (
    <div>
        <div>.</div>
        <div>.</div>
        <div>.</div>
    </div>
  )
}

export default Options